# Post links implementation

1. Add and test a pure HTTP(S) link normalizer.
2. Preserve `link` in `ApiPost`, `FeedPost`, and `mapFeedPost`.
3. Render an accessible link card in `PostCard`.
4. Verify focused tests, TypeScript, lint, and the final diff.
