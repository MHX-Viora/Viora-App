# Kế hoạch giao diện xác thực và khởi động

1. Tạo nền và nhận diện dùng chung cho các màn hình xác thực.
2. Đổi màu thẻ, trường nhập và nút sang hệ màu cộng đồng.
3. Thêm màn hình chờ trong lúc khôi phục phiên và đồng bộ màu native splash.
4. Chạy lint, TypeScript và kiểm tra diff.
