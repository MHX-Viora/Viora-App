# Plan: Create short-video post

1. Extend the existing selection step with camera/microphone permissions and video capture.
2. Add working flip-camera, torch, timer, start/stop controls while preserving library upload and next-step actions.
3. Apply the futuristic cyan-purple visual treatment to both create steps and update native permission descriptions.
4. Pass captured files through the existing preview, metadata, upload, and feed mutation flow.
5. Verify TypeScript, lint, permissions, recording lifecycle, and final diff quality.
