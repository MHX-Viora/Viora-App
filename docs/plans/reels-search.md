# Plan: Reels search

1. Give `ReelsHeader` an explicit accessible search callback.
2. Add a full-screen search component with input, results, clear, empty, and close states.
3. Connect visibility to `ReelsScreen` and pause playback while search is open.
4. Run TypeScript, lint, and final diff review.
