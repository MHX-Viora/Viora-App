# Plan: Media interactions

1. Move feature models to `types/` and raise typography tokens.
2. Install and configure SDK 54 media packages.
3. Build a create-post modal and connect composer actions/feed state.
4. Replace Reel poster rendering with `expo-video` and mixed-size sources.
5. Run type-check, lint, web export, and review dead imports.
