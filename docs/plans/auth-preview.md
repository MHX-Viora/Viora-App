# Plan: Authentication preview

1. Create shared accessible auth field and primary-button controls.
2. Build login and registration screens with responsive keyboard-safe layouts.
3. Add Expo Router routes and register them in the root stack.
4. Wire Profile settings logout to `/login`.
5. Run lint, TypeScript, and focused diff checks.

