# Viora

Ứng dụng mạng xã hội mobile xây dựng bằng Expo Router và React Native.

## Chạy dự án

```bash
npm install
npm run start
```

`app/` chỉ chứa route. UI tái sử dụng nằm trong `components/`; screen, dữ liệu và type được nhóm theo tính năng trong `features/`.
