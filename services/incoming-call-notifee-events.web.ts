// Notifee is native-only. SignalR call events are still handled by the shared
// realtime service without registering native notification event listeners.
export const setupIncomingCallNotifeeEvents = () => undefined;
